# Copy to .env. Never commit .env or paste keys into GitHub comments.
LLM_PROVIDER=auto
OPENAI_API_KEY=
LLM_MODEL=
MOCK_MODE=false
TTS_PROVIDER=edge
# Optional compatible provider:
GROQ_API_KEY=
REQUEST_TIMEOUT=35
