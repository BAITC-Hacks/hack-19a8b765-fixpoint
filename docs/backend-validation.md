# Проверка backend

2026-09-23, Python 3.12, Windows. Ключ Groq отсутствует.

- `python -m pytest -q`: 71 проверка прошли. Внешние LLM/STT/TTS здесь не вызываются.
- Валидированы 40 сценариев, 3 системных намерения, 43 слота, 31 действие, 13 разделов KB,
  11 клиентов; ссылки, дата среза, двуязычные вопросы и отсутствие дубликатов.
- Dev: 104 реплики — 52 RU, 45 KK, 7 mixed; 84 single, 13 multi-intent, 4 out_of_scope,
  3 unclear. Все ожидаемые ID существуют.
- Проверены суммы 38000, 15400, 163800; preview без записи, запрет записи без согласия,
  изменение параметров, отказ, повтор операции, изоляция, очередь и возврат к теме.
- HTTP-контракт и WebSocket frontend проверены с подменой внешнего провайдера:
  ID событий, повтор request_id, отсутствие действий при сбое LLM.
- Проверен вход в каждый из 40 сценариев. 10 размеченных диалогов использованы как
  smoke-тест контроллера с готовыми решениями. Это не оценка LLM и не полная приёмка диалогов.

`python scripts/evaluate_router.py` остановлен из-за отсутствия GROQ_API_KEY.
Primary accuracy, full match и multi-intent recall **не измерены**. Predictions не выдумывались.
После добавления ключа тот же скрипт сохранит reports/predictions.json,
reports/routing_trace.json и reports/evaluation.txt, вызвав исходный evaluate.py.

Живые RU/KK/mixed, задержки провайдера и браузерный TTFA не проверены. Docker-файлы
подготовлены, Docker-сборка здесь не запускалась. Ограничения моков описаны в decisions.md.
